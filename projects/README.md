Drop your SLR project JSON files here, each with its PDFs (e.g. in a pdfs/ subfolder).

Then open:  http://localhost:8080/?project=/projects/<your-project>.json

An example is included: /projects/project.example.json
